# Web framework
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Data validation / settings
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# HTTP client (Ollama calls, sitemap fetching)
requests==2.32.4

# Vector store — ChromaDB (active)
chromadb==1.5.7

# Vector store — PostgreSQL/pgvector (dormant, ready for migration)
pgvector==0.3.6
psycopg[binary]==3.2.9

# Website crawler (JavaScript-rendered pages)
playwright==1.59.0

# HTML parsing
beautifulsoup4==4.13.4

# PDF ingestion
pypdf==5.9.0
